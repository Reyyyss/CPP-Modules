# CPP Module 01

**Topics:** Memory allocation, pointers to members, references, file streams

This module covers:
- Memory allocation and deallocation (`new`, `delete`)
- Pointers to members
- References
- File streams (`ifstream`, `ofstream`)
